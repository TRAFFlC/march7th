# 项目面试讲解指南：三月七语音对话系统（七音盒 Music7ox）

> 用途：面试时介绍个人项目。按"定位 → 亮点实现 → 技术决策 → 追问应对"组织，全部基于本仓库真实实现。

---

## 1. 一句话定位

**一个全栈本地优先的 AI 角色扮演语音对话系统**：FastAPI + Vue3 + Electron 三端一体，用本地 LLM（Ollama）+ GPT-SoVITS 语音合成 + RAG 知识库，实现"能听、能说、有记忆、会成长"的虚拟角色（三月七），并提供桌面宠物形态的常驻交互入口。

## 2. 技术栈速览

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python 3.12 / FastAPI / Uvicorn | API 服务，SSE 流式 |
| 数据 | MySQL 8（用户/会话/反馈）+ ChromaDB（向量库） | 结构化与向量双存储 |
| LLM | Ollama（deepseek-r1:8b）+ OpenAI 兼容 API 双通 | 本地优先，云端可切换 |
| 语音 | GPT-SoVITS v2ProPlus | 角色音色克隆与情绪化合成 |
| 检索 | sentence-transformers + BM25 + RRF + CrossEncoder 重排 | 混合检索链路 |
| 前端 | Vue3 + Vite + Pinia + ECharts | 会话管理、数据可视化 |
| 桌宠 | Electron 28 + 本地 KWS/ASR（Python 子进程） | 常驻桌面、语音唤醒 |
| 工程 | Docker Compose、pytest、压力测试、配置热重载 | 可部署可验证 |

## 3. 值得重点讲的 8 个实现

### 3.1 RAG 混合检索：BM25 + 向量 + RRF 融合 + CrossEncoder 重排
- **解决什么问题**：角色知识（设定、剧情、关系网）单一检索路径召回差——纯向量检索对专有名词（"星穹列车"、"三月七"）不敏感，纯 BM25 不懂语义。
- **怎么做**（`inference.py`）：向量召回 + jieba 分词建 BM25 索引 → RRF（`Σ 1/(k+rank)`，k=60）融合两路排名 → 距离阈值过滤 → CrossEncoder 对 query-doc 逐对精打分重排 → Top-K 注入上下文。
- **体现的思考**：粗排求召回、精排求精度、融合求鲁棒的经典检索架构，且全部本地可跑、零外部依赖。
- **可能追问**："为什么不直接用 top_k 向量检索？" → 用排名倒数融合而非分数拼接，避免两路分数量纲不可比。

### 3.2 情绪感知语音合成
- **解决什么问题**：同一句话"我没事"，开心和难过读法完全不同；固定音色的 TTS 毫无角色感。
- **怎么做**：回复生成后先做情绪分类（happy/sad/angry/confused/excited/neutral），再按情绪切换 GPT-SoVITS 的参考音频与语气样本合成（`emotion_classifier.py` + `tts_service.py`），情绪配置由角色 JSON 声明。
- **体现的思考**：把"人格一致性"从纯文本层扩展到声学层；情绪分类支持独立轻量 API 配置（与主对话模型解耦）。

### 3.3 反馈闭环：让角色"越聊越对"
- **解决什么问题**：LLM 角色扮演最大痛点是人设漂移和事实错误，一次性调 prompt 治标不治本。
- **怎么做**（`rag_iteration.py` + `api/routers/rag.py`）：低分评分 → 用户选择问题类型（事实错误/偏离角色/遗忘历史/思考泄露，`feedback_types.py`）→ 独立 LLM 按类型生成结构化修正建议 → **人工确认后**才写回 RAG 知识库 → 下次检索即可纠正。
- **体现的思考**：数据飞轮 + 人工卡点。确认机制防止低质量修正污染知识库；类型化反馈让 LLM 分析有明确目标而非泛泛而谈。

### 3.4 独立迭代 API 与多 Provider 降级
- **解决什么问题**：本地 8B 模型"分析自己的错误"能力不足；外部 API 有 429 限流。
- **怎么做**（`rag_iteration.py` `get_iteration_api_config`）：角色可配置专用 `iteration_apis`（仅允许云端兼容 API，排除 ollama），多个配置按序降级重试；`llm_provider.py` 实现 429 指数退避 + `Retry-After` 解析。
- **体现的思考**：不同任务用不同模型（对话用本地保隐私，分析用云端保质量）；生产级的限流容错意识。

### 3.5 用户画像与偏好学习（时间衰减）
- **解决什么问题**：用户兴趣会变，上月喜欢的 topic 这月未必；全量统计会稀释近期信号。
- **怎么做**（`user_preference.py` + `profile_summary.py`）：偏好关键词按 `权重 = 计数 × e^(-0.1×天数)` 指数衰减；对话 token 超阈值自动生成画像摘要（兴趣/风格/情感倾向），注入后续对话上下文。
- **体现的思考**：个性化 ≠ 无限堆历史；用衰减 + 摘要控制上下文成本，兼顾"记得你"和"上下文窗口有限"的现实约束。

### 3.6 流式体验：SSE + 分句 TTS 边生成边播
- **解决什么问题**：LLM 生成 + TTS 合成双重延迟，整段等待体验极差。
- **怎么做**：`POST /api/chat/stream` 以 SSE 推送 `text`/`audio`（base64 分段）/`done` 事件，前端打字机渲染 + 音频队列顺序播放。
- **体现的思考**：语音场景的"首字节延迟"优化——把流水线切成流。

### 3.7 Electron 桌宠：语音唤醒的常驻入口
- **解决什么问题**：网页端必须开浏览器找标签页，"陪伴感"断裂。
- **怎么做**（`desktop_pet/`）：置顶透明悬浮窗 + 系统托盘；本地 KWS 唤醒词检测（Python 子进程 `models/kws_service.py`）→ 唤醒后停 KWS 让麦、启离线 ASR → 识别结果直接对话 → TTS 回播。唤醒词可按角色配置。
- **体现的思考**：桌面常驻 + 免手动 = 真正"叫她就能应"的入口；KWS/ASR 麦克风资源互斥调度是工程细节亮点。

### 3.8 多用户与工程化
- JWT 认证 + 角色权限（普通用户/管理员）、会话级历史持久化（`dialogue_sessions` 表，服务重启可恢复）、角色配置热重载（后台线程监控 + 回调）、Docker Compose 一键部署（MySQL/后端/前端/Nginx + 可选 Ollama/GPT-SoVITS profile）、pytest 单测 + 并发压力测试。

## 4. 建议的讲述结构（3 分钟版）

1. **痛点**（30s）：AI 角色扮演三个老大难——人设漂移、无长期记忆、语音延迟。
2. **方案**（60s）：本地优先全栈架构图（前端/后端/LLM/TTS/RAG/桌宠六块），强调"数据不出本机"。
3. **深挖一个亮点**（60s）：首选反馈闭环（3.3）或混合检索（3.1）——前者讲产品思维（数据飞轮+人工卡点），后者讲技术深度（召回-融合-重排）。
4. **结果与数字**（30s）：功能清单 + 检索链路各环节 + 部署方式，收尾一句"全部模块自己实现，没有套 LangChain/character.ai 脚手架"。

## 5. 高频追问与回答要点

| 追问 | 回答要点 |
|---|---|
| 为什么不用 LangChain/LlamaIndex？ | 链路不长（检索→融合→重排→注入），自研可控可调参（RRF 的 k、阈值、rerank 时机），也避免黑盒排障困难；这也是学习价值的来源。 |
| 本地 8B 模型效果够吗？ | 分任务：对话够用（有 RAG 补知识）；自我纠错分析不够——所以设计了独立迭代 API 让云端强模型干分析，本地模型干生成，各司其职。 |
| 延迟多少？ | 已做 SSE 流式 + 分句 TTS，首段可听延迟从"整段生成+合成"降为"首句生成+首句合成"；桌宠端还有 KWS 唤醒 ~亚秒级。 |
| RAG 写坏数据怎么办？ | 双保险：人工确认卡点（confirmed 才入库）+ 写入相似度去重；规划中再加来源/置信分级（auto 建议弱信任）。 |
| 多用户隔离？ | MySQL 按用户分表存储会话/偏好/反馈；RAG 分静态角色知识与动态用户知识两类集合。 |
| 最难的 bug？ | 可讲：emotions/emotion_images 键名不匹配导致情绪配置静默丢失——教训是配置解析要做兼容与显式校验；或 TTS 参考音频硬编码问题。 |

## 6. 一页速记（面试前 5 分钟看）

- **链路**：语音输入 → ASR → RAG 混合检索 → LLM（含用户画像注入）→ 情绪分类 → 分句 TTS → SSE 推流
- **三个关键词**：混合检索（BM25+向量+RRF+重排）、反馈闭环（类型化→建议→确认→回写）、本地优先（Ollama+GPT-SoVITS，云端仅做分析增强）
- **架构**：FastAPI + Vue3 + Electron + MySQL + ChromaDB
- **数字**：6 种情绪音色、4 类反馈类型、30s 配置热重载检查、0.1 时间衰减率
