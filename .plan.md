# 修复计划

## 问题概览

### 1. TTS DeprecationWarning 修复
**文件**: `api/main.py`
**问题**: 使用已废弃的 `datetime.utcnow()` 
**位置**: 第351, 1116, 1200, 1293, 3034行
**解决方案**: 替换为 `datetime.now(datetime.UTC)`

### 2. 角色TTS配置缺失
**文件**: `config/characters.json`
**问题**: TTS字段全为空值，导致"未指定参考音频且接口无预设"错误
```json
"tts_config": {
  "gpt_weight": "",
  "sovits_weight": "",
  "ref_audio_path": "",
  "ref_audio_text": ""
},
"emotions": {}
```
**解决方案**: 
- 设置GPT/SoVITS模型路径
- 设置默认参考音频（neutral情绪）
- 从情绪文件夹自动填充emotions配置

### 3. 角色编辑页面缺少TTS配置面板
**文件**: `frontend/src/views/Admin.vue`
**问题**: 角色编辑模态框（第316-361行）只有LLM/RAG配置，缺少TTS参数
**需要添加**:
- GPT权重路径输入
- SoVITS权重路径输入
- 默认参考音频/文本
- 语速控制
- 情绪配置面板（可动态添加）

### 4. 情绪文件夹自动读取
**文件**: 新增API端点 + `character_config.py`
**数据结构**:
- 情绪文件夹: `resources/emotions/march7th/{emotion}/march7th/`
- 情绪音频: 每个文件夹一个`.wav`文件
- 对话文本: `dialogues.txt` 格式为 `line_number|text`
- 音频-文本映射: `slicer_opt.list` 格式为 `audio_path|slicer_opt|ZH|text`

**解决方案**:
1. 创建新API端点 `/api/admin/characters/{id}/emotions/scan`
2. 扫描指定路径下的情绪文件夹
3. 解析`slicer_opt.list`获取音频-文本映射
4. 返回情绪配置建议JSON

### 5. 个人数据不显示问题
**涉及文件**: 
- `api/main.py` - `/api/user/preference/stats`端点
- `frontend/src/views/Personal.vue` - 个人数据页面
- `database.py` - `get_preference_stats`函数

**问题分析**:
- `user_preferences`表需要在用户评分>=4时才会保存偏好
- `extract_and_save_preferences()`在`voice_chat.py`第283, 324-334行调用
- 前端可能未正确处理空数据状态

**解决方案**:
1. 检查`user_preference.py`模块
2. 确认数据库表结构正确
3. 添加调试日志查看数据流
4. 确保前端正确处理空数据

### 6. 调试面板不显示问题
**涉及文件**:
- `api/main.py` - `/api/admin/debug-info`端点
- `voice_chat.py` - `process_user_input`方法
- `frontend/src/views/Admin.vue` - 调试面板

**问题分析**:
- `_LAST_DEBUG_INFO`全局变量仅在成功对话后设置
- TTS错误可能导致提前返回，未设置debug_info
- 需要在所有代码路径都设置debug_info

**解决方案**:
1. 在API端点添加fallback逻辑
2. 确保错误情况下也设置debug_info
3. 添加对话历史调试信息

### 7. RAG输出在LLM生成后（不改变工作流程）
**涉及文件**:
- `voice_chat.py` - `process_user_input`方法
- `api/main.py` - 对话API端点响应结构

**当前流程**: 保持不变
```
用户输入 -> RAG检索 -> LLM生成 -> 返回结果
```

**新增功能**: 在API响应中额外返回RAG检索详情
```json
{
  "response": "LLM生成的回复文本",
  "emotion": "happy",
  "rag_info": {
    "query": "用户问题",
    "documents": [
      {
        "content": "检索到的文档片段",
        "similarity": 0.85,
        "metadata": {...}
      }
    ],
    "total_found": 3,
    "filtered_count": 2
  },
  "debug_info": {...}
}
```

**前端改动**: 在对话气泡下方显示可折叠的RAG信息面板

## 实施顺序

1. ✅ 修复DeprecationWarning（最简单）
2. ✅ 修复角色TTS配置（填充模型路径和情绪参考音频）
3. ✅ 实现情绪文件夹自动读取API
4. ✅ 添加角色编辑TTS面板
5. ✅ 调查个人数据显示问题
6. ✅ 调查调试面板问题
7. ✅ 添加RAG输出信息到API响应（不改变工作流程）

## 技术细节

### 情绪配置JSON结构
```json
{
  "emotions": {
    "neutral": {
      "ref_audio_path": "resources/emotions/march7th/neutral/march7th/vocal_xxx.wav",
      "ref_text": "对应的文本内容"
    },
    "happy": { ... },
    "confused": { ... },
    "sad": { ... },
    "angry": { ... },
    "excited": { ... }
  }
}
```

### slicer_opt.list格式
```
audio_filename|slicer_opt|ZH|对应文本
```

### dialogues.txt格式
```
行号|文本内容
```
